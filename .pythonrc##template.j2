import site
import unicodedata
import os


{%- for pkg, import, in (("loguru","from loguru import logger"),("python-box","from box import Box"),("flame","import flame"),("jinja2", "from jinja2 import Template, Environment, ChoiceLoader, BaseLoader, FileSystemLoader"))  -%}
try:
  {{ import }}
except ImportError:
    run(f"{sys.executable} -mpip install {{ pkg }}", shell=True, capture_output=True)
finally:
    {{ import }}
{%- endfor -%}
